{
  "name": "http-ws",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc -b",
    "start": "node dist/index.js",
    "dev": "pnpm run build && pnpm run start"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@repo/mynewpackage": "workspace:*"
  },
  "dependencies": {
    "@types/express": "^5.0.3",
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "zod": "^4.0.5"
  }
}
